<h3 class="ui header" style="margin-top: 1em; text-align: center;"><%= site.categories.length %> nodes now and growing.</h3>
               <div class="ui divider"></div>
             </div>
           </div>
           <div class="ui fluid card">
             <div class="content" style="font-size: 14px !important;"><%= __('node.lists') %></div>
             <div class="content">
<% site.categories.each(function(category){ %>
               <a class="ui basic circular site-node label" href="<%- url_for(category.path) %>"><%= category.name %></a>
<% }) %>
